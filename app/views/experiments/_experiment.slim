.experiment data-id=(experiment.id)
  .line = text_or_button(experiment, :assumption, 'Make an assumption')
  .line = text_or_button(experiment, :result, 'Log results') if experiment.assumption.present?
  .line = text_or_button(experiment, :action, 'Take action') if experiment.result.present?